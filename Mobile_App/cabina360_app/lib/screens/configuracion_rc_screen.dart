import 'package:flutter/material.dart';

class ConfiguracionRCScreen extends StatelessWidget {
  const ConfiguracionRCScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ConfiguracionRCScreen'),
      ),
    );
  }
}
