import 'package:flutter/material.dart';

class ConfiguracionCamaraScreen extends StatelessWidget {
  const ConfiguracionCamaraScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ConfiguracionCamaraScreen'),
      ),
    );
  }
}
